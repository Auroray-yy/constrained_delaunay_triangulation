#version 460
layout(local_size_x = 64) in;

layout(std430, binding = 0) buffer edge_map_buffer {
    uint maps[]; 
};
uniform int clear_size;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx < clear_size) {
        maps[int(idx)] = -1;
    }
}
